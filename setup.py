from setuptools import setup


if __name__ == "__main__":
    setup(
        dependency_links = [
            "build_helper/talib.whl"
        ],
        install_requires = [
            "build_helper/talib.whl"
        ]
    )
